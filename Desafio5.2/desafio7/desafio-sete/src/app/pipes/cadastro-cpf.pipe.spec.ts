import { CadastroCpfPipe } from './cadastro-cpf.pipe';

describe('CadastroCpfPipe', () => {
  it('create an instance', () => {
    const pipe = new CadastroCpfPipe();
    expect(pipe).toBeTruthy();
  });
});
